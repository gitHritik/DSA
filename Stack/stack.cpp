#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define rep(i, a, b) for (int i = a; i < b; i++)
int32_t main()
{
    /*
    stack<int> s;

    s.push(4);
    s.push(2);

    cout << s.top() << endl;
    s.pop();
    cout << s.top() << endl;

    if (s.empty())
    {
        cout << "Empty array";
    }
    else
    {
        cout << "Not empty array";
    }
    s.pop();
    if (s.empty())
    {
        cout << "Empty array";
    }
    else
    {
        cout << "Not empty array";
    }
    */
}