#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define rep(i, a, b) for (int i = a; i < b; i++)
void UniqueOccurence(int arr[], int n)
{
    int new[n];
    int count = 0;
}
int32_t main()
{
    int size;
    cin >> 
    int arr[100];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    UniqueOccurence(arr, size);
}